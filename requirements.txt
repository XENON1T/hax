# You also need the packages below, but they won't install via pip
# numpy
# numba           # Need to import it to avoid ROOT import error :-(
# pandas
# pax
# ROOT
mock
tqdm
cloudpickle
dask>=0.11.0
multihist>=0.5.0
pytz
parsedatetime
